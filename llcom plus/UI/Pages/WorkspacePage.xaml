<Page x:Class="llcom_plus.Pages.WorkspacePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Loaded="Page_Loaded" Title="Workspaces">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,10">
            <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock x:Name="DescriptionText" TextWrapping="Wrap" Margin="0,6,0,0" Opacity="0.75"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NameBox" MaxLength="64" VerticalContentAlignment="Center" MinWidth="100"/>
            <Button x:Name="SaveButton" Grid.Column="1" Margin="8,0,0,0" Padding="12,6" Click="Save_Click"/>
        </Grid>
        <ListView x:Name="WorkspaceList" Grid.Row="2" SelectionChanged="WorkspaceList_SelectionChanged"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4,5">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Summary}" FontSize="12" Opacity="0.7" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <WrapPanel Grid.Row="3" Margin="0,8,0,4">
            <Button x:Name="LoadButton" Padding="12,6" Margin="0,0,6,4" Click="Load_Click"/>
            <Button x:Name="ImportButton" Padding="12,6" Margin="0,0,6,4" Click="Import_Click"/>
            <Button x:Name="ExportButton" Padding="12,6" Margin="0,0,6,4" Click="Export_Click"/>
            <Button x:Name="DeleteButton" Padding="12,6" Margin="0,0,6,4" Click="Delete_Click"/>
        </WrapPanel>
        <StackPanel Grid.Row="4">
            <TextBlock x:Name="DetailsText" TextWrapping="Wrap" Margin="0,4,0,6"/>
            <CheckBox x:Name="ScriptReferencesCheck" Margin="0,4"/>
            <TextBlock x:Name="ScriptNoticeText" TextWrapping="Wrap" FontSize="12" Opacity="0.75" Margin="0,2,0,4"/>
        </StackPanel>
        <TextBlock x:Name="StatusText" Grid.Row="5" TextWrapping="Wrap" Margin="0,6,0,0"
                   AutomationProperties.LiveSetting="Polite"/>
    </Grid>
</Page>
