# 日志查找与快捷指令流程

## 在主日志中查找

点击收发日志上方的“查找”，或按 Ctrl+F。输入文字后用 Enter / Shift+Enter 找下一项 / 上一项，Aa 区分大小写，.* 使用正则，Esc 关闭。分屏时查找当前选中的串口。

查找只暂停视图自动滚动，不停止串口接收或日志保存。点击“刷新”把新收到的日志加入查找。范围为当前窗口保留的最近最多 1,048,576 个字符，最多显示 2,000 个匹配。

点击“快捷发送”旁边的“时间线”页签，使用 COM、收发类型、文本 / 原始 HEX / 正则筛选、书签与 CSV 导出，不再挤占主日志高度。Ctrl+选择两条记录查看间隔；通知中心双击一条通知可切换到该页并定位对应端口附近的时间。时间线最多保留最近 10,000 条 / 16 MiB，主机观测时间不等同于设备执行时间或硬件功耗测量。主日志显示格式与时间线的原始数据可以不同。

在“更多设置”中切换“显示发送内容（TX）”只影响当前串口的日志视图：隐藏期间仍保留有界历史，已经启用的日志文件仍记录 TX；重新勾选可恢复保留范围内的历史 TX。清空日志或超出历史容量而淘汰的记录不会恢复。

“发送与日志选项”中的“显示回车换行标记”默认开启。取消勾选只隐藏自动生成的 `\r`、`\n` 文字标记，保留实际换行；当前串口保留的历史会立即刷新，重新勾选可恢复标记。不会改变实际收发数据、接收脚本、HEX 视图或日志文件，数据中原本存在的反斜杠文字也不会删除。该设置按 COM 保存，分屏时作用于当前选中的串口。

快捷指令不再显示数字编号。按住左侧六点手柄上下拖动，蓝色插入线表示松手后的位置；靠近列表上下边缘会自动滚动。松手后自动保存并沿用现有快照机制，Esc 或拖出列表取消；文本框仍正常编辑，不会误触拖动。也可聚焦手柄后按 Alt+↑ / ↓ 排序。仅在当前页内调整顺序，不改指令内容、脚本或等待回复设置，旧脚本使用的内部编号保持兼容。交互参考 [Notion 的块手柄](https://www.notion.com/help/duplicate-delete-and-restore-content)。

单击六点手柄，在右侧半圆菜单中选择“复制”或“删除”；按住并拖动仍然排序，Esc 或点击外部关闭菜单。复制保留整条指令和流程设置；删除沿用自动快照与工具栏撤销保护。键盘聚焦手柄后按 Enter / 空格可打开菜单。

“更多设置”中的“日志前缀”可按 COM 配置日期、时间、毫秒、端口名称，以及发送/接收标识（允许留空）；预览即时更新，并可恢复默认。更改后已有分包日志按新配置重绘，自动保存与手动导出仍保留标准时间和方向。连续文本模式不插入额外前缀，避免打断原始数据流。

## 快捷发送：收到回复后继续

1. 按执行顺序在同一页添加指令。
2. 点击每条指令右侧设置，展开“流程：等待回复”。
3. 选择包含文本、完整一行或正则匹配，填写期望回复与超时。
4. 选择并打开一个明确的 COM，点击“运行本页流程”，确认执行范围。

例如 FTP 初始化可配置为：

| 步骤 | 指令内容 | 等待条件 |
| --- | --- | --- |
| 1 | 模块手册指定的 FTP 账号配置指令 | 完整一行 `OK` |
| 2 | 模块手册指定的 FTP 打开连接指令 | 实际的最终连接成功回复 |
| 3 | 模块手册指定的切换目录指令 | 实际的目录切换成功回复 |

不同模块的 AT 指令和成功提示不同，示例只说明配置顺序，不应照搬其它模块的指令。FTP 打开后可能先回 `OK`、稍后才上报连接结果；第二步应等待最终结果，不能只等中间的 `OK`。

- 普通“发送”仍然只发一条，不会自动执行后续指令。
- 默认不等待回复的旧指令，流程中在实际发送完成后继续；运行确认会显示未验证回复的条数。
- 默认重试为 0。超时、连接变化、断开或发送异常会停止，不改发其它 COM。设置重试前请确认该指令可重复执行；发送异常不会自动重试。
- 停止会取消尚未发送的剩余内容与后续步骤，不能撤回已经发给设备的字节。匹配原始接收文本，区分大小写，不自动区分主动上报和命令回复。“完整一行”要求收到行结束符；没有换行的模块可选择“包含文本”或正则。
- 运行采用开始时的指令快照；编辑、排序和切页只影响下次运行。需要排除的指令可勾选“流程中跳过”。
- 新设置随原有自动快照、复制、导入导出和工作区一起保存；旧默认快照的校验格式保持兼容。

## 其它工作流入口

- 曲线：选择串口和数据格式，配置字段、名称、单位和颜色；支持单值、CSV、键值对、JSON、正则以及原脚本加点方式。串口数据按换行解析，暂停显示不停止采集。
- 工作区：保存串口配置、分屏布局和快捷指令。恢复前必须关闭串口与运行任务，并成功创建恢复前快照。恢复不自动打开串口或执行脚本；脚本引用默认不启用。
- 低功耗诊断：记录发送、DTR 唤醒、引脚和通知事件，帮助核对软件行为；不能替代电流仪或逻辑分析仪。

设计参考：[Wireshark 主界面筛选工具栏](https://www.wireshark.org/docs/wsug_html_chunked/ChUseFilterToolbarSection.html)、[Docklight 发送后等待接收序列](https://docklight.de/manual/dl_waitforsequence.html)。
